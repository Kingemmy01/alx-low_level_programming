#include <stdio.h>

/**
 * main - A program that prints all strings from 0 to 9
 * Return: Always O (Success)
 */
int main(void)
{
	int num;

	for (num = 0; < 10; num++)
		printf("%d", num);

	printf("\n");

	return (0);
}
